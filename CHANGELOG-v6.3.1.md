# Event Access Platform v6.3.1

## Correções

- Atualiza `InvitationService` para preencher os novos campos de evento do `TicketView`.
- Atualiza `PaymentService` para preencher os novos campos de evento do `TicketView`.
- Corrige a inferência de `List<TicketView>` no fluxo de consulta de pagamento.
- Mantém todas as funcionalidades da v6.3: check-in por TKT/token/URL, download PNG/QR e páginas legais.
