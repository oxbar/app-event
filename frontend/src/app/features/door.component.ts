import {ChangeDetectionStrategy,Component,DestroyRef,ElementRef,ViewChild,inject,signal} from '@angular/core';import {FormBuilder,ReactiveFormsModule,Validators} from '@angular/forms';import {BrowserQRCodeReader} from '@zxing/browser';import {TuiButton,TuiInput} from '@taiga-ui/core';import {TuiSelect} from '@taiga-ui/kit';import {CheckinApi,EventApi} from '../core/api.services';import {AccessPoint,CheckinResult,EventModel} from '../core/models';
@Component({standalone:true,imports:[ReactiveFormsModule,TuiButton,TuiInput,TuiSelect],template:`<div class="page-title"><div><h1>Portaria</h1><p>Leitura mobile-first com prevenção de acesso duplicado.</p></div></div><section class="door-layout"><div class="panel"><label>Evento</label><tui-textfield><select tuiSelect (change)="selectEvent($event)">@for(e of events();track e.id){<option [value]="e.id">{{e.name}}</option>}</select></tui-textfield><label>Portaria</label><tui-textfield><select tuiSelect (change)="pointId.set($any($event.target).value)">@for(p of points();track p.id){<option [value]="p.id">{{p.name}}</option>}</select></tui-textfield><video #video></video><div class="button-row"><button tuiButton type="button" (click)="start()">Iniciar câmera</button><button tuiButton appearance="secondary" type="button" (click)="stop()">Parar</button></div><form [formGroup]="form" (ngSubmit)="manual()"><tui-textfield><input tuiInput placeholder="Cole ou digite o código" formControlName="token" /></tui-textfield><button tuiButton type="submit">Validar manualmente</button></form></div>@if(result();as r){<article class="scan-result" [class.approved]="r.approved"><strong>{{r.approved?'ENTRADA LIBERADA':'ENTRADA NEGADA'}}</strong><h2>{{r.attendeeName}}</h2><p>{{r.ticketType}} · {{r.wristbandLabel}}</p><p>{{r.message}}</p></article>}</section>`,changeDetection:ChangeDetectionStrategy.OnPush})export class DoorComponent{@ViewChild('video')video?:ElementRef<HTMLVideoElement>;private eventsApi=inject(EventApi);private api=inject(CheckinApi);private fb=inject(FormBuilder);private reader=new BrowserQRCodeReader();private controls?:{stop():void};readonly events=signal<EventModel[]>([]);readonly points=signal<AccessPoint[]>([]);readonly eventId=signal('');readonly pointId=signal('');readonly result=signal<CheckinResult|null>(null);readonly form=this.fb.nonNullable.group({token:['',Validators.required]});constructor(){this.eventsApi.list().subscribe(p=>{this.events.set(p.content);if(p.content[0])this.loadPoints(p.content[0].id)})}selectEvent(e:Event){this.loadPoints((e.target as HTMLSelectElement).value)}loadPoints(id:string){this.eventId.set(id);this.api.points(id).subscribe(p=>{this.points.set(p);this.pointId.set(p[0]?.id??'')})}async start(){if(!this.video)return;this.controls=await this.reader.decodeFromVideoDevice(undefined,this.video.nativeElement,(r)=>{if(r){this.scan(r.getText());this.stop()}})}stop(){this.controls?.stop()}manual(){this.scan(this.form.getRawValue().token)}scan(token:string){if(!this.eventId()||!this.pointId())return;this.api.scan(this.eventId(),token,this.pointId()).subscribe(r=>{this.result.set(r);if(r.approved)navigator.vibrate?.(150);else navigator.vibrate?.([100,80,100])})}}
